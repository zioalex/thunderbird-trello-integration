{
  "name": "trello-task-creator-thunderbird",
  "version": "1.1.12",
  "description": "Thunderbird extension to create Trello tasks",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint *.js",
    "lint:fix": "eslint *.js --fix",
    "validate": "npm run lint && npm run test",
    "build": "npm run validate && npm run package",
    "package": "rm -f trello-task-creator-extension.zip && mkdir -p dist && cp -r manifest.json background.js popup.html popup.js options.html options.js icons/ README.md dist/ && cd dist && zip -r ../trello-task-creator-extension.zip . && cd .. && rm -rf dist",
    "version:patch": "./scripts/bump-version.sh patch",
    "version:minor": "./scripts/bump-version.sh minor",
    "version:major": "./scripts/bump-version.sh major",
    "version": "./scripts/bump-version.sh",
    "release": "./scripts/create-release.sh",
    "release:tag": "./scripts/create-release.sh"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.1.2",
    "web-ext": "^8.9.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/tests/setup.js"
    ],
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "*.js",
      "!jest.config.js",
      "!coverage/**"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/thunderbird-trello-extension.git"
  },
  "keywords": [
    "thunderbird",
    "trello",
    "extension",
    "webextension"
  ],
  "author": "Your Name",
  "license": "MIT"
}
